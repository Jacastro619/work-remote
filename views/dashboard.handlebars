{{! Dashboard is populated with user's job listing }}
{{#if posts}}
  {{#each posts}}
    <div class="d-inline-flex justify-content-around flex-wrap">
      <div
        class="card m-2"
        style="width: 20rem;  min-height: 450px; max-height: 450px"
      >
        <img
          class="card-img-top"
          src="https://des.az.gov/sites/default/files/job-searching.png?time=1644301518"
          alt="Card image cap"
        />
        <div class="card-body">
          <h5 class="card-title">{{job_title}}</h5>
          <h6 class="card-title">{{job_type}}</h6>
          <p class="card-text">{{post_tease job_description}}....</p>
          <a href="/jobs/{{id}}" class="btn btn-info">Learn More</a>
          <a href="#" class="btn btn-warning ml-1" id="editBtn">Edit</a>
          <button
            data-id="{{id}}"
            class="btn btn-danger float-right"
            id="deleteBtn"
          >Delete</button>
        </div>
      </div>
    </div>
  {{/each}}
{{/if}}
<script src="/js/delete.js"></script>